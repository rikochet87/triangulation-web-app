{
  "name": "triangulation-web-app",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /triangulation-web-app",
    "deploy": "gh-pages -d build",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "dependencies": {
    "gh-pages": "^4.0.0",
    "parcel": "^2.6.2"
  }
}
